{
	"exempt": [
		{ "route": "/dev/ui", "reason": "component showcase, dev-only, not content" },
		{ "route": "/pcd", "reason": "client-side redirect stub to /pcd/{preference}/" }
	],
	"pending": [
		{ "route": "/", "reason": "home" },
		{
			"route": "/pcd/*",
			"reason": "PCD entity browser, remaining mirror layers not yet built; regular expression detail mirrors are live and guaranteed by their own prerender"
		}
	]
}
